// To find the second largest number in an array

const SecondLargest = (input, place) => {
  if (!input) {
    return 'Enter Valid input';
  }
  let largest;
  let secondLargest;
  for (let i = 0; i < input.length; i++) {}
};

console.log(SecondLargest([1, 2, 3, 4, 7, 8, 9, 5], 2));
